-- cheet.nvim plugin loader
-- This file is auto-loaded by Neovim

if vim.g.loaded_cheet then
  return
end
vim.g.loaded_cheet = true

-- The main module creates commands when required
-- Users should call require('cheet').setup() in their config
